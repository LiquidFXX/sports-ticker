name: Wire NFL game center once

on:
  push:
    paths:
      - '.github/workflows/nfl-game-center-once.yml'
      - 'scripts/nfl_game_center_patch_once.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Apply coordinator patch
        run: python scripts/nfl_game_center_patch_once.py

      - name: Compile integration
        run: python -m compileall -q custom_components/sports_ticker

      - name: Run unit tests
        run: python -m unittest discover -s tests -v

      - name: Commit validated update
        shell: bash
        run: |
          git diff --check
          rm -f scripts/nfl_game_center_patch_once.py
          rm -f .github/workflows/nfl-game-center-once.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'Expose normalized NFL live game center data'
          git push origin HEAD:main
